package sudoku.domain;

/**
 * Interface for Column, which is semantically same as row. Interface is used to distinct different Sudoku board concepts from each other.
 */
public interface Column extends GroupOfCells {
}
